dist: xenial
sudo: true
language: python
python:
    - '3.6'
cache: pip
install:
    - pip install -r requirements.txt
    - pip install -U pytest pytest-cov codecov
script:
    - pytest ardent/ tests/
after_success:
    - codecov
